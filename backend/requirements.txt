# API-serving dependencies only — keep the serverless function lean.
# Data-loading dependencies (yfinance, openpyxl, requests) live in
# requirements-data.txt; loaders run locally or in Docker, not on Vercel.
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
sqlalchemy>=2.0.43,<2.1
psycopg[binary]>=3.2.13,<4
redis>=6.1,<7
pydantic>=2.12,<3
pydantic-settings>=2.10,<3
python-dateutil>=2.9,<3
